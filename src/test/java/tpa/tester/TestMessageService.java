//-----------------------------
//-- Generated By Denny(luhox@qq.com)
//-- Remarks: service tester
//-- Date Generated: 2020-10-09 10:48:00
//-----------------------------

package tpa.tester;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;
import java.math.BigDecimal;
import org.junit.Test;
import java.util.Date;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import com.baomidou.mybatisplus.core.conditions.update.UpdateWrapper;
import tpa.entity.Message;
import tpa.service.IMessageService;

@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = "classpath:spring/spring-context.xml")
public class TestMessageService {
	@Autowired
	@Qualifier("messageService")
	private IMessageService messagesvc;

	@Transactional
	@Test
	@Rollback(true)
	public void testExecute() throws Exception {

		// ---------------增加功能测试-------------------
		Message it = new Message();
		it.setFrom("mm");
		it.setTo("mm");
		it.setUsername("mm");
		it.setEmail("mm");
		it.setMsg("mm");
		it.setCreate_gmt(new Date().getTime());
		it.setUpdate_gmt(0l);
		try {
			messagesvc.save(it);
		} catch (Exception e) {
			e.printStackTrace();
		}
		// 验证
		Message item = messagesvc.getById(it.getId());
		System.out.println("it="+it.toString());
		System.out.println("item="+item.toString());
		assertTrue(item.getFrom().equals(it.getFrom()));

		// ---------------修改功能测试-------------------
		UpdateWrapper<Message> ituw = new UpdateWrapper<>();
		ituw.set(Message.FROM , "hello!");
		ituw.eq(Message.ID , it.getId());		
		boolean itupdate = messagesvc.update(ituw);	
		// 验证
		item = messagesvc.getById(it.getId());
		assertTrue(item.getFrom().equals("hello!"));

		// ---------------删除功能测试-------------------
		messagesvc.removeById(it.getId());
		item = messagesvc.getById(it.getId());
		assertEquals(item, null);

	}
}
